package graph;

import graph.model.EWGraph;

public class NumberOfWaysToArriveAtDestination {
/*
    int[] numpath = new int[G.V];
    numpath[0] = 1;
    while (!queue.isEmpty()) {
        //System.out.println(queue);
        int u = queue.poll();
        outOfQueue[u] = true;
        for (EWGraph.Edge e : G.adj(u)) {
            int w = e.other(u);
            //System.out.println(distTo[e.w]+" "+distTo[u]);
            if (!outOfQueue[w]) {
                double d = distTo[u] + e.weight;
                if (d < distTo[w]) {
                    numpath[w] = numpath[u];
                    distTo[w] = d;
                    edgeTo[w] = e;
                    queue.remove(w); // make queue to rearrange
                    queue.add(w);
                }else if(d == distTo[w]){
                    numpath[w] = (numpath[w]+numpath[u])%1_000_000_007;
                }
            }
        }
    }
    public static void main(String[] args){

    }

 */
}
