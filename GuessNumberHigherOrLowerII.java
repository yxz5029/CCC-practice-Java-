public class GuessNumberHigherOrLowerII {
    public static void main(String[] args) {
        int n = 10;
        dp = new int[n + 1][n + 1];

        // init value for DP

        int r = guess(1, n);
    }

    static int[][] dp; // dp[i][j] means the money for range i to j

    static int guess(int i, int j) {
        return i;
    }
}
